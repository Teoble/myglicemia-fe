import Logo from '../components/Logo';
import Settings from '../components/Settings';

const Header = () => {
  return (
    <header>
      <Settings />
      <Logo />
    </header>
  );
};

export default Header;
